# External Blockers

The following work cannot be truthfully completed from this local workspace without owner-provided accounts, publication decisions, or media:

1. Publish a stable dashboard/MCP deployment and record its public URL.
2. Configure a production database, identity model, retention/deletion policy, and transport authentication before exposing the service beyond localhost.
3. Create the public under-three-minute YouTube demo with voiceover explaining the product, Codex workflow, and GPT-5.6/model-provider usage.
4. Publish or grant judging access to the code repository and verify the license choice.
5. Enter the Devpost category, project description, repository URL, video URL, screenshots, and `/feedback` session ID.
6. Register an MCP Apps application identity and deploy its asset bundle if an embedded MCP Apps UI is desired; do not claim this capability before then.
7. Provide a real OpenAI or OpenRouter test credential in a controlled environment if live model-provider smoke testing is required.

These are deliberately not represented as completed in the README or verification report.
